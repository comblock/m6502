; I chose these numbers because this way, if you NOT the direction, the result is the oposite direction;
; I actually didn't need this, for some reason I thought that when the snake wrapped around 
; I would need to invert the direction but that would just make the snake crash into itself.
UP    = 3
DOWN  = 0
LEFT  = 1
RIGHT = 2
